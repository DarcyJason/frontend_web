# 字体

```css
p {
  /* 设置字体, 可以去google上搜索 */
  font-family: Arial, Helvetica, sans-serif;
  /* 设置字体样式 */
  font-style: italic;
  /* 设置字体粗细 */
  font-weight: bold;
  /* 设置字体大小 */
  font-size: 20px;
}
```
